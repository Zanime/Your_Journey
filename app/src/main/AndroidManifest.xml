<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.imperial.london.yourjourney">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StepFreeActivity"></activity>
        <activity android:name=".JourneyDisplayActivity" />
        <activity
            android:name=".LauncherActivity"
            android:configChanges="orientation"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.NoDisplay"
            android:windowSoftInputMode="stateHidden" />
        <activity android:name=".PreferencesActivity" />
        <activity android:name=".UserInputActivity" />
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DownloadedActivity" />
        <activity android:name=".StationDetailActivity" />
        <activity android:name=".TubeMapActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".DirectoryActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>